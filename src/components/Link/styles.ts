import { StyleSheet } from 'react-native';

import { Colors } from '@styles';

export default StyleSheet.create({
  base: {
    borderBottomWidth: 1,
    alignSelf: 'flex-start',
    borderBottomColor: Colors.CuriousBlue,
  },
});
